# Minimal client config for testing bidirectional tunnel
log_level: "info"
tls_skip_verify: true
server_addr: "127.0.0.1"
server_port: 10086
client_group_id: "test-group"

# Enable egress (forward proxy) - client listens on port 8002
http_entry_port: 8002
